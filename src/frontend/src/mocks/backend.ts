import type { backendInterface, MenuCategory, MenuItem, Order, OrderItem, Reservation, CustomerProfile, ChatThread, ChatMessage, Table } from "../backend";
import { DietaryTag, OrderStatus, OrderType, ReservationStatus, SenderRole, TableStatus } from "../backend";
import { Principal } from "@icp-sdk/core/principal";

const dummyPrincipal = Principal.anonymous();

const menuCategories: MenuCategory[] = [
  { id: BigInt(1), name: "Starters", description: "Begin your journey", emoji: "🥗" },
  { id: BigInt(2), name: "Main Course", description: "Signature dishes", emoji: "🍽️" },
  { id: BigInt(3), name: "Desserts", description: "Sweet endings", emoji: "🍮" },
  { id: BigInt(4), name: "Drinks", description: "Curated beverages", emoji: "🥂" },
  { id: BigInt(5), name: "Chef's Special", description: "Exclusive creations", emoji: "⭐" },
];

const menuItems: MenuItem[] = [
  {
    id: BigInt(1),
    categoryId: BigInt(1),
    name: "Truffle Arancini",
    description: "Crispy risotto balls with black truffle and aged parmesan, served with saffron aioli",
    price: BigInt(1800),
    imageUrl: "https://images.unsplash.com/photo-1541614101331-1a5a3a194e92?w=400",
    tags: [DietaryTag.vegetarian],
    isAvailable: true,
    isPopular: true,
    calories: BigInt(320),
  },
  {
    id: BigInt(2),
    categoryId: BigInt(1),
    name: "Wagyu Beef Carpaccio",
    description: "Paper-thin sliced A5 wagyu with capers, rocket, and truffle vinaigrette",
    price: BigInt(2800),
    imageUrl: "https://images.unsplash.com/photo-1559847844-5315695dadae?w=400",
    tags: [DietaryTag.chefsSpecial],
    isAvailable: true,
    isPopular: true,
    calories: BigInt(280),
  },
  {
    id: BigInt(3),
    categoryId: BigInt(2),
    name: "Lobster Thermidor",
    description: "Half Maine lobster with cognac cream sauce, gruyère gratin, served with truffle frites",
    price: BigInt(6800),
    imageUrl: "https://images.unsplash.com/photo-1559847844-5315695dadae?w=400",
    tags: [DietaryTag.chefsSpecial],
    isAvailable: true,
    isPopular: true,
    calories: BigInt(580),
  },
  {
    id: BigInt(4),
    categoryId: BigInt(2),
    name: "Dry-Aged Ribeye",
    description: "45-day dry-aged USDA prime ribeye with bordelaise sauce and roasted bone marrow",
    price: BigInt(8500),
    imageUrl: "https://images.unsplash.com/photo-1558030006-450675393462?w=400",
    tags: [],
    isAvailable: true,
    isPopular: true,
    calories: BigInt(720),
  },
  {
    id: BigInt(5),
    categoryId: BigInt(2),
    name: "Pan-Seared Duck Confit",
    description: "Slow-cooked duck leg with cherry gastrique, pomme sarladaise, and microgreens",
    price: BigInt(4200),
    imageUrl: "https://images.unsplash.com/photo-1432139555190-58524dae6a55?w=400",
    tags: [DietaryTag.glutenFree],
    isAvailable: true,
    isPopular: false,
    calories: BigInt(620),
  },
  {
    id: BigInt(6),
    categoryId: BigInt(3),
    name: "Valrhona Chocolate Fondant",
    description: "Warm dark chocolate lava cake with salted caramel ice cream and gold leaf",
    price: BigInt(1600),
    imageUrl: "https://images.unsplash.com/photo-1563805042-7684c019e1cb?w=400",
    tags: [DietaryTag.vegetarian],
    isAvailable: true,
    isPopular: true,
    calories: BigInt(480),
  },
  {
    id: BigInt(7),
    categoryId: BigInt(4),
    name: "Vintage Bordeaux Selection",
    description: "Sommelier-curated glass from our cellar of 500+ labels",
    price: BigInt(2200),
    imageUrl: "https://images.unsplash.com/photo-1510812431401-41d2bd2722f3?w=400",
    tags: [DietaryTag.vegan],
    isAvailable: true,
    isPopular: false,
    calories: BigInt(120),
  },
  {
    id: BigInt(8),
    categoryId: BigInt(5),
    name: "The youCallIT Experience",
    description: "A 7-course tasting menu crafted personally by our executive chef — tell us your preferences",
    price: BigInt(18500),
    imageUrl: "https://images.unsplash.com/photo-1414235077428-338989a2e8c0?w=400",
    tags: [DietaryTag.chefsSpecial],
    isAvailable: true,
    isPopular: true,
    calories: BigInt(1200),
  },
];

const sampleTable: Table = {
  id: BigInt(1),
  number: BigInt(5),
  capacity: BigInt(4),
  status: TableStatus.available,
  x: BigInt(100),
  y: BigInt(150),
};

const sampleOrder: Order = {
  id: BigInt(1),
  customerId: dummyPrincipal,
  items: [
    { menuItemId: BigInt(1), quantity: BigInt(2), specialInstructions: "" },
    { menuItemId: BigInt(3), quantity: BigInt(1), specialInstructions: "Extra sauce please" },
  ],
  status: OrderStatus.confirmed,
  orderType: OrderType.dineIn,
  subtotal: BigInt(10400),
  tax: BigInt(1040),
  total: BigInt(11440),
  specialInstructions: "",
  tableNumber: BigInt(5),
  createdAt: BigInt(Date.now()) * BigInt(1_000_000),
};

const sampleReservation: Reservation = {
  id: BigInt(1),
  customerId: dummyPrincipal,
  customerName: "Alexandra Chen",
  tableId: BigInt(5),
  guestCount: BigInt(2),
  date: "2026-04-20",
  time: "19:30",
  status: ReservationStatus.confirmed,
  email: "alexandra@example.com",
  phone: "+1 (555) 234-5678",
  specialOccasion: "Anniversary",
  notes: "Window table preferred",
};

const sampleChatMessage: ChatMessage = {
  id: BigInt(1),
  senderId: dummyPrincipal,
  senderRole: SenderRole.chef,
  content: "Hello! I'm Chef Marco. Your table is ready and we have some wonderful specials tonight. How can I make your evening exceptional?",
  timestamp: BigInt(Date.now()) * BigInt(1_000_000),
  isRead: false,
  orderId: BigInt(1),
};

const sampleChatThread: ChatThread = {
  id: BigInt(1),
  customerId: dummyPrincipal,
  orderId: BigInt(1),
  messages: [sampleChatMessage],
  lastActivity: BigInt(Date.now()) * BigInt(1_000_000),
  isOpen: true,
};

const sampleProfile: CustomerProfile = {
  id: dummyPrincipal,
  name: "Alexandra Chen",
  email: "alexandra@example.com",
  phone: "+1 (555) 234-5678",
  dietaryPreferences: [DietaryTag.glutenFree],
  favoriteItems: [BigInt(3), BigInt(8)],
};

export const mockBackend: backendInterface = {
  addFavoriteItem: async () => true,
  addMenuCategory: async (name, description, emoji) => ({
    id: BigInt(6),
    name,
    description,
    emoji,
  }),
  addMenuItem: async (categoryId, name, description, price, imageUrl, tags, calories) => ({
    id: BigInt(27),
    categoryId,
    name,
    description,
    price,
    imageUrl,
    tags,
    calories: calories ?? undefined,
    isAvailable: true,
    isPopular: false,
  }),
  cancelOrder: async () => true,
  cancelReservation: async () => true,
  deleteMenuItem: async () => true,
  getAllCustomers: async () => [sampleProfile],
  getAllOpenThreads: async () => [sampleChatThread],
  getAllOrders: async () => [sampleOrder],
  getAllReservations: async () => [sampleReservation],
  getAllTables: async () => Array.from({ length: 15 }, (_, i) => ({
    id: BigInt(i + 1),
    number: BigInt(i + 1),
    capacity: i % 3 === 0 ? BigInt(6) : i % 2 === 0 ? BigInt(4) : BigInt(2),
    status: i % 4 === 0 ? TableStatus.occupied : i % 7 === 0 ? TableStatus.reserved : TableStatus.available,
    x: BigInt(((i % 5) * 120) + 60),
    y: BigInt((Math.floor(i / 5) * 120) + 60),
  })),
  getAvailableTables: async () => [sampleTable, { ...sampleTable, id: BigInt(2), number: BigInt(8) }],
  getCustomerOrders: async () => [sampleOrder],
  getCustomerReservations: async () => [sampleReservation],
  getMenuCategories: async () => menuCategories,
  getMenuItem: async (id) => menuItems.find(i => i.id === id) ?? null,
  getMenuItems: async () => menuItems,
  getMenuItemsByCategory: async (categoryId) => menuItems.filter(i => i.categoryId === categoryId),
  getMyProfile: async () => sampleProfile,
  getNewMessages: async () => [],
  getOrder: async () => sampleOrder,
  getOrdersByStatus: async () => [sampleOrder],
  getReservation: async () => sampleReservation,
  getThread: async () => sampleChatThread,
  getThreadByOrder: async () => sampleChatThread,
  makeReservation: async (tableId, guestCount, date, time, customerName, phone, email, specialOccasion, notes) => ({
    id: BigInt(2),
    customerId: dummyPrincipal,
    tableId,
    guestCount,
    date,
    time,
    customerName,
    phone,
    email,
    status: ReservationStatus.confirmed,
    specialOccasion: specialOccasion ?? undefined,
    notes: notes ?? undefined,
  }),
  markMessagesRead: async () => true,
  modifyReservation: async () => true,
  placeOrder: async (items, orderType, specialInstructions, tableNumber) => ({
    id: BigInt(2),
    customerId: dummyPrincipal,
    items,
    orderType,
    specialInstructions,
    tableNumber: tableNumber ?? undefined,
    status: OrderStatus.confirmed,
    subtotal: BigInt(5000),
    tax: BigInt(500),
    total: BigInt(5500),
    createdAt: BigInt(Date.now()) * BigInt(1_000_000),
  }),
  removeFavoriteItem: async () => true,
  searchMenuItems: async (term) => menuItems.filter(i =>
    i.name.toLowerCase().includes(term.toLowerCase()) ||
    i.description.toLowerCase().includes(term.toLowerCase())
  ),
  sendChefMessage: async (orderId, content) => ({
    id: BigInt(2),
    senderId: dummyPrincipal,
    senderRole: SenderRole.chef,
    content,
    timestamp: BigInt(Date.now()) * BigInt(1_000_000),
    isRead: false,
    orderId,
  }),
  sendMessage: async (orderId, content) => ({
    id: BigInt(3),
    senderId: dummyPrincipal,
    senderRole: SenderRole.customer,
    content,
    timestamp: BigInt(Date.now()) * BigInt(1_000_000),
    isRead: false,
    orderId,
  }),
  toggleItemAvailability: async () => true,
  updateMenuItem: async () => true,
  updateOrderStatus: async () => true,
  updateProfile: async (name, phone, email, dietaryPreferences) => ({
    id: dummyPrincipal,
    name,
    phone,
    email,
    dietaryPreferences,
    favoriteItems: [],
  }),
  updateReservationStatus: async () => true,
  updateTableStatus: async () => true,
};
